#include <stdio.h>
#include <string.h>
#include <stdlib.h>

#define READDY "ready"
int main(void)
{
    unsigned char cmd[1024] = {("abc",)( "1")};
    sprintf(cmd, )
    printf("%s\n", cmd);
    printf("%s\n", READDY);
    return 0;
}
